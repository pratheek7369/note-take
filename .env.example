# Copy to .env and set real values before deploying
# Production login URL for authentication
VITE_LOGIN_URL=https://apis.ccbp.in/login

# If you use a remote MockAPI for notes, set the base URL (optional)
# VITE_NOTES_API_URL=https://your-mockapi.mockapi.io/api/v1/notes
