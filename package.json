{
  "name": "note-taking-app",
  "version": "1.0.0",
  "description": "Note-Taking Web Application with JWT Authentication",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "vite": "^5.0.8",
    "@vitejs/plugin-react": "^4.2.1"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.6",
    "concurrently": "^8.2.0"
  },
  "scripts": {
    "dev": "vite",
    "mock": "json-server --watch db.json --port 3001",
    "dev:all": "concurrently \"npm run mock\" \"npm run dev\"",
    "build": "vite build",
    "vercel-build": "vite build",
    "preview": "vite preview"
  }
}
